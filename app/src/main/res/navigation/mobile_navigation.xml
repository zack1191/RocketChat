<?xml version="1.0" encoding="utf-8"?>
<navigation
    xmlns:android = "http://schemas.android.com/apk/res/android"
    xmlns:app = "http://schemas.android.com/apk/res-auto"
    xmlns:tools = "http://schemas.android.com/tools"
    android:id = "@+id/mobile_navigation"
    app:startDestination="@id/nav_contact">

    <fragment
        android:id="@+id/nav_contact"
        android:name = "com.hex.chattie.ui.all.AllFragment"
        android:label  ="Contacts"
        tools:layout = "@layout/fragment_all"/>

    <fragment
        android:id="@+id/nav_group"
        android:name = "com.hex.chattie.ui.group.GroupFragment"
        android:label  ="Group"
        tools:layout = "@layout/fragment_group"/>

    <fragment
        android:id="@+id/nav_channels"
        android:name = "com.hex.chattie.ui.channels.ChannelsFragment"
        android:label  ="Channels"
        tools:layout = "@layout/fragment_channels"/>

</navigation>